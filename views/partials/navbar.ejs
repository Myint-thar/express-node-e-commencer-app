<nav x-data="{ shopOpen: false, searchOpen: false, profileOpen: false, wishlistOpen: false, cartOpen: false }" class="bg-white shadow-sm sticky top-0 z-50">
  <!-- Top Announcement -->
  <div class="bg-slate-900 text-white text-xs py-2 px-6 flex justify-between items-center border-b border-slate-800">
    <span>✈️ Free Worldwide Shipping on Orders Over $50</span>
    <span>🛡️ 30-Day Money Back Guarantee</span>
    <span>🎧 24/7 Customer Support</span>
  </div>

  <div class="py-4 px-8 flex justify-between items-center border-b relative">
    <!-- Logo -->
    <a href="/products" class="text-2xl font-black tracking-wider text-slate-900">
      <span class="text-orange-500">FAM</span>SWORLD
    </a>
    
    <!-- Nav Links -->
    <div class="hidden md:flex gap-8 text-xs font-bold tracking-wide items-center">
      <a href="/products" class="hover:text-orange-500">HOME</a>
      
      <!-- Shop Dropdown -->
      <div class="relative py-2" @mouseenter="shopOpen = true" @mouseleave="shopOpen = false">
        <button class="hover:text-orange-500 flex items-center gap-1">
          SHOP <span>▼</span>
        </button>
        <div x-show="shopOpen" x-cloak x-transition class="absolute top-full left-0 w-48 bg-white border shadow-lg rounded-sm py-2 z-50">
          <a href="/products/category/electronics" class="block px-4 py-2 text-xs text-gray-700 hover:bg-orange-50 hover:text-orange-500">Electronics</a>
          <a href="/products/category/fashion" class="block px-4 py-2 text-xs text-gray-700 hover:bg-orange-50 hover:text-orange-500">Fashion</a>
          <a href="/products/category/beauty" class="block px-4 py-2 text-xs text-gray-700 hover:bg-orange-50 hover:text-orange-500">Beauty & Care</a>
        </div>
      </div>

      <a href="/products/best-sellers" class="hover:text-orange-500">BEST SELLERS</a>
      <a href="/products/new-arrivals" class="hover:text-orange-500">NEW ARRIVALS</a>
      <a href="/about" class="hover:text-orange-500">ABOUT US</a>
      <a href="/contact" class="hover:text-orange-500">CONTACT US</a>
    </div>

    <!-- Action Icons -->
    <div class="flex items-center gap-6 text-slate-700 text-lg relative">
      <!-- Search Hover -->
      <div class="relative py-2" @mouseenter="searchOpen = true" @mouseleave="searchOpen = false">
        <button class="hover:text-orange-500 flex items-center">🔍</button>
        <div x-show="searchOpen" x-cloak x-transition class="absolute right-0 top-full w-72 bg-white p-3 border shadow-xl rounded-md z-50">
          <form action="/products/search" method="GET" class="flex items-center border rounded px-2 py-1 bg-gray-50">
            <input type="text" name="q" placeholder="Search products..." class="w-full text-xs bg-transparent focus:outline-none">
            <button type="submit" class="text-xs text-orange-500 font-bold px-1">Go</button>
          </form>
        </div>
      </div>

      <!-- Account Dropdown -->
      <div class="relative">
        <button @click="profileOpen = !profileOpen" class="hover:text-orange-500 flex items-center">👤</button>
        <div x-show="profileOpen" @click.outside="profileOpen = false" x-cloak x-transition class="absolute right-0 mt-2 w-48 bg-white border shadow-lg rounded-md py-2 z-50">
          <% if (currentUser) { %>
            <div class="px-4 py-2 border-b">
              <p class="text-xs font-bold text-slate-900"><%= currentUser.name || 'User' %></p>
              <p class="text-[10px] text-gray-400"><%= currentUser.email %></p>
            </div>
            <a href="/users/profile" class="block px-4 py-2 text-xs hover:bg-gray-100">My Profile</a>
            <a href="/users/orders" class="block px-4 py-2 text-xs hover:bg-gray-100">My Orders</a>
            <hr class="my-1">
            <a href="/users/logout" class="block px-4 py-2 text-xs text-red-500 hover:bg-red-50">Logout</a>
          <% } else { %>
            <div class="p-3 text-center">
              <a href="/users/login" class="block w-full bg-orange-500 text-white font-bold text-xs py-1.5 rounded mb-1 hover:bg-orange-600">Login</a>
              <a href="/users/register" class="block w-full border font-bold text-xs py-1.5 rounded hover:bg-gray-50">Register</a>
            </div>
          <% } %>
        </div>
      </div>

      <!-- Wishlist Drawer -->
      <div class="relative">
        <button @click="wishlistOpen = !wishlistOpen" class="hover:text-orange-500 relative flex items-center">
          ❤️
          <span class="absolute -top-2 -right-2 bg-orange-500 text-white text-[9px] rounded-full h-4 w-4 flex items-center justify-center font-bold">
            <%= typeof wishlistItems !== 'undefined' ? wishlistItems.length : 0 %>
          </span>
        </button>
        <div x-show="wishlistOpen" @click.outside="wishlistOpen = false" x-cloak x-transition class="absolute right-0 mt-2 w-64 bg-white border shadow-xl rounded-md p-3 z-50">
          <h4 class="font-bold text-xs text-slate-900 border-b pb-2 mb-2">Saved Wishlist</h4>
          <p class="text-xs text-gray-400 text-center py-2">View all items in your account.</p>
        </div>
      </div>

      <!-- Cart Drawer -->
      <div class="relative">
        <button @click="cartOpen = !cartOpen" class="hover:text-orange-500 relative flex items-center">
          🛒
          <span class="absolute -top-2 -right-2 bg-orange-500 text-white text-[9px] rounded-full h-4 w-4 flex items-center justify-center font-bold">
            <%= typeof cartItems !== 'undefined' ? cartItems.length : 0 %>
          </span>
        </button>
        <div x-show="cartOpen" @click.outside="cartOpen = false" x-cloak x-transition class="absolute right-0 mt-2 w-72 bg-white border shadow-xl rounded-md p-3 z-50">
          <h4 class="font-bold text-xs text-slate-900 border-b pb-2 mb-2">Shopping Cart</h4>
          <p class="text-xs text-gray-400 text-center py-2">Your cart items list here.</p>
        </div>
      </div>
    </div>
  </div>
  <div x-show="wishlistOpen" @click.outside="wishlistOpen = false" x-cloak x-transition class="absolute right-0 mt-2 w-80 bg-white border shadow-xl rounded-md p-4 z-50">
  <h4 class="font-bold text-xs text-slate-900 border-b pb-2 mb-3 flex justify-between items-center">
    <span>Saved Favorites</span>
    <span class="text-orange-500"><%= typeof wishlistItems !== 'undefined' ? wishlistItems.length : 0 %> Items</span>
  </h4>

  <div class="max-h-60 overflow-y-auto space-y-3">
    <% if (typeof wishlistItems !== 'undefined' && wishlistItems.length > 0) { %>
      <% wishlistItems.forEach(item => { %>
        <div class="flex items-center justify-between gap-3 border-b pb-2">
          <img src="<%= item.image %>" class="w-10 h-10 object-contain rounded border">
          <div class="flex-1 text-left">
            <p class="text-xs font-bold text-slate-800 line-clamp-1"><%= item.title %></p>
            <p class="text-[11px] text-orange-500 font-bold">$<%= item.price %></p>
          </div>

          <form action="/wishlist/remove/<%= item.id || item._id %>" method="POST">
            <button type="submit" class="text-red-500 hover:text-red-700 font-bold text-xs bg-red-50 hover:bg-red-100 px-2 py-1 rounded transition">
              ✕
            </button>
          </form>
        </div>
      <% }) %>
    <% } else { %>
      <p class="text-xs text-gray-400 text-center py-4">Saved Favorites ထဲတွင် မည်သည့်ပစ္စည်းမှ မရှိသေးပါ။</p>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
  const urlParams = new URLSearchParams(window.location.search);
  const msg = urlParams.get('msg');

  if (msg) {
    let textMsg = "လုပ်ဆောင်ချက် အောင်မြင်ပါသည်!";

    if (msg === 'cart_added') textMsg = "Cart ထဲသို့ အောင်မြင်စွာ ထည့်ပြီးပါပြီ။";
    if (msg === 'cart_removed') textMsg = "Cart ထဲမှ အောင်မြင်စွာ ဖျက်လိုက်ပါပြီ။";
    if (msg === 'wishlist_added') textMsg = "Favorites ထဲသို့ အောင်မြင်စွာ သိမ်းဆည်းလိုက်ပါပြီ။";
    if (msg === 'wishlist_removed') textMsg = "Favorites ထဲမှ အောင်မြင်စွာ ဖျက်လိုက်ပါပြီ။";

    Swal.fire({
      title: 'မင်းအောင်မြင်ပါတယ်!',
      text: textMsg,
      icon: 'success',
      timer: 2000,
      showConfirmButton: false
    });

    // URL မှ ?msg= Parameter ကို ပြန်ရှင်းပေးခြင်း
    window.history.replaceState({}, document.title, window.location.pathname);
  }
</script>


  
</nav>